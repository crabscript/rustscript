main = {SOI ~ program ~ EOI}

program = _{ (seq ~ expr?) | expr }

seq = _{ expr_stmt ~ (expr_stmt)* }
expr = @{ (ASCII_DIGIT)+ }
expr_stmt = { expr ~ SEMICOLON}

SEMICOLON = _{ ";" }
WHITESPACE = _{ (" " | NEWLINE)+}
COMMENT = _{ "/*" ~ (!"*/" ~ ANY)* ~ "*/" }